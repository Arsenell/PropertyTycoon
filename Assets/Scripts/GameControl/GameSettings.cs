using UnityEngine;

public static class GameSettings
{
    // Total number of human players selected from the dropdown
    public static int playerCount;

    // Array to store the selected sprites for each player
    public static Sprite[] playerSprites; // Up to 6 players
}